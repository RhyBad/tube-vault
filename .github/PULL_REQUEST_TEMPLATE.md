<!-- Code PRs are NOT accepted (see CONTRIBUTING.md). This PR should be TRANSLATIONS or DOCS only. -->
## Type
- [ ] Translation (`locales/<lang>.json`)
- [ ] Docs (`docs/`)

## Notes
<!-- Translators: keys unchanged, values translated, partial is fine (falls back to English). -->
