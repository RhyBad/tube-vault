# Configuration
Environment variables (see `.env.example`) — secret gate, DB/Redis URLs, `VAULT_ROOT`,
`TUBEVAULT_CREDENTIAL_KEY_FILE` (enables the credential feature), `DEFAULT_LOCALE`.
Volumes: media vault, credential keys, postgres data. TODO: fill in exact vars from the upstream image.
