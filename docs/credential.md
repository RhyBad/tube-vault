# YouTube credential (cookies)
For members-only / age-restricted content, import your YouTube cookie jar (Netscape format) in Settings.
Set `TUBEVAULT_CREDENTIAL_KEY_FILE` to enable the feature. If it expires, only members-only/age-gated
downloads pause — public downloads keep running. The cookie is stored encrypted and never re-read (status only).
