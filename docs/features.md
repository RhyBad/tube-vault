# Features
User-facing overview (translated from the product capability). Preservation-first: archives channels,
detects/captures live streams, flags **Rescued** copies (kept after the original vanished), storage +
cleanup, notifications/activity log. TODO: expand per release.
